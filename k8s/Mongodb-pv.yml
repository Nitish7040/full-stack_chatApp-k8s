apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pv
    # namespace: chat-app
spec:
  storageClassName: standard
  capacity:
    storage: 4Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data